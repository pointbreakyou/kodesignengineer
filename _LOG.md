# _ATLAS-LOG — Design Engineer Website Portfolio (сайт інженера-конструктора Костянтина)

| Дата | Що зроблено | Файли |
|---|---|---|
| 2026-09-18 | 🌐 **Підключено власний домен overchenko.engineering** (реєстратор NIC.UA, замовлення 2856315). Зона заведена в Cloudflare (Free), NS julissa+wilson.ns.cloudflare.com. Після імпорту вичищено 37 сміттєвих записів парковки (3 A на 135.181.41.169 вкл. wildcard + 34 NS-делегації на parked*.uadns.com) — інакше апекс і субдомени лишились би на парковці. Домен прив язано до Pages-проєкту overchenkoengineering (апекс + www), DNS: CNAME @ і www → overchenkoengineering.pages.dev (proxied). Redirect Rule www → апекс 301, SSL=Full (strict), Always Use HTTPS=on. ⚠️ Знайдено і виправлено SEO-баг: усі 61 canonical/OG/JSON-LD/sitemap/robots вели на **мертвий** kodesignengineer.pages.dev (не на живий хост) → переписано на https://overchenko.engineering. Також оновлено видимі рядки (футер лід-магніту, тема і «Джерело» листів з форми) і бампнуто SW-кеш .08→.09. ⚠️ Урок: у панелі NIC.UA список власних NS зберігається окремо від типу NS — поки тип «Паркувальні», реєстр віддає parked*, хоч список Cloudflare уже заповнений. | site/**/*.html, site/robots.txt, site/sitemap.xml, site/js/form-brief.js, site/js/lead-magnet.js, site/service-worker.js; Cloudflare zone overchenko.engineering |
| 2026-07-19 | 🖱️ Скролбар прибрано ПОВНІСТЮ (не тонкий стиль, а `display:none`/`scrollbar-width:none` на html+body) — прокрутка лишається доступною тільки колесом миші/тачпадом (Lenis все одно перехоплює wheel). ⚠️ Урок: сайт має Service Worker (`service-worker.js`, cache-first для CSS/JS) — після кожної правки стилів **обов'язково бампати `CACHE` версію**, інакше браузер користувача продовжує показувати стару закешовану CSS навіть після успішного деплою (саме тому перша правка скролбару «не подіяла» — SW віддавав старий style.css з кешу). Бампнув `ko-engineer-v2-2026.04→.05→.06`. | `site/css/style.css`, `site/service-worker.js` |
| 2026-07-19 | 🔗 Футер-кредит «Розроблено/Developed by Overchenko Studio» (10 файлів: index/portfolio/drawings/certificates/blog × ua+en) перелинковано зі старого `overchenkostudio.netlify.app` на живий домен нової студії **`https://overchenko.studio`**. Прибрано скрол-прогрес-бар «micrometer» (top-fixed 3px смуга, заповнювалась по scroll) — HTML-розмітка з 10 сторінок, JS-логіка з `js/main.js` (`updateMicro`, Lenis-колбек), CSS-блок `.micrometer/.micrometer__fill/.micrometer__ticks` зі `style.css`. Задеплоєно на прод `overchenkoengineering.pages.dev` (`wrangler pages deploy site`), верифіковано live через curl (0 згадок micrometer, домен studio оновлено). | всі HTML у `site/`, `site/en/`, `site/js/main.js`, `site/css/style.css` |
| 2026-06-12 | 🚀 **ДЕПЛОЙ на Cloudflare Pages + фінальні правки.** Картки послуг: кнопки уніфіковано на **«Обговорити проєкт»** (en «Discuss the project»). Форма заявки: бюджет повернуто діапазонами, але **в гривнях** — «До 10 000 / 10 000–25 000 / 25 000–50 000 / 50 000–100 000 / Понад 100 000 грн» (en — ті ж у UAH). Деплой: проєкт **`overchenkoengineering`** (Cloudflare Pages, акаунт kostik.bk.ua, БЕЗ тире — Костянтин попросив; старий `overchenko-engineering` з тире видалено) → **https://overchenkoengineering.pages.dev**. ⚠️ Блокер 25 MiB: у `site/assets` лежали 35 CAD-вихідників `.bip` (211 МБ, 4 з них >25 МБ) → винесено в `_cad-sources/` поза `site/` (не вебфайли; `.bip.N.jpg` рендери лишились). `.assetsignore` для `pages deploy` НЕ працює — лише фізичне винесення. Верифіковано live: root/en/blog 200, portfolio/drawings/certificates 308→200 (чисті URL), 0 цінників, «Обговорити проєкт» і «Понад 100 000 грн» присутні. Урок: `BRAIN\knowledge\lessons\cloudflare-pages-25mib-cad-sources.md` | `site/index.html`, `site/en/index.html`; `_cad-sources/**` (винесені .bip); Cloudflare project `overchenko-engineering` |
| 2026-06-12 | 💸 **Прибрано ВСІ ціни з сайту** (укр + en дзеркало) — перехід на модель «вартість індивідуально під задачу». Картки послуг: «ВІД $80/$250/$900» → «ВАРТІСТЬ · Індивідуально» (en «PRICE · On request»). Блок-калькулятор переоформлено в **конфігуратор без цін**: вивід «$min—$max» → «Параметри зафіксовано», eyebrow «ОРІЄНТОВНА ВАРТІСТЬ»→«ВАШ ЗАПИТ», CTA «Отримати прорахунок»→«Обговорити вартість», секція «02/ПРОРАХУНОК»→«02/КОНФІГУРАТОР», меню/футер/герой «Прорахунок»→«Конфігуратор». FAQ Q1 (відповідь без сум), Q2 (прибрано «+30%/+60% вартості»). Форма брифу: дропдаун бюджету $-діапазони → текстові рівні (Стартовий/Робочий/Розширений/Комплексний). JSON-LD `priceRange` видалено. Блог-картка «Ціна проєкту…» перейменована. `calculator.js` очищено від цінової логіки (BASE_MIN/MAX, min/max, urgMult) — лишив тип/складність/обсяг/термін + автозаповнення брифу без ціни. CSS: `.service__price--quote` (менший шрифт, one-line). **Верифіковано Preview :3100**: 0 цінників у grep, 0 помилок консолі, калькулятор працює (термін/тип оновлюються, бриф без ціни), скріни послуг+конфігуратора укр ОК, en перевірено через fetch. | `site/index.html`, `site/en/index.html`, `site/js/calculator.js`, `site/css/style.css`, `site/blog/index.html` |

## 2026-09-18 — services.html: чистий кадр 01
Прибрано SVG-накладку `data-over="0"` над рендером 3D-моделі (розмірні лінії H/L, виноски «ВУЗОЛ A · ЗВАРНИЙ» / «ВУЗОЛ B · ГВИНТ»). Причина: написи перекривали й псували рендер. Накладки 02 (карта напружень) і 03 (штамп аркуша) залишені. JS (`services.js`) працює без змін — `overFor()` повертає null і все відфільтровується.

## 2026-09-18 — services.html: аудит + прохід полірування

**Критичне (сторінка була зламана на телефоні).**
`@media (max-width:1100px)` скидав `grid-column` тільки для `.stage__rail`, але не для
`.stage__frame` і `.stage__panel`. Вони лишалися в колонках 2 і 3 неіснуючої сітки →
браузер створював неявні колонки нульової ширини: `grid-template-columns: 287px 0px 0px`.
Уся сцена «одна деталь, чотири стани» — рендер + картка стану — була **0px завширшки**,
тобто невидима на мобайлі. Виправлено: `grid-column: 1` для обох + `minmax(0,1fr)`.
Там же: `.page-hero__stats` мав 4 колонки на 375px (переповнення 552 > 335) → 2×2 у style.css.
Горизонтальний виліт документа (`body.scrollWidth` 730 при 375) прибрано через
`overflow-x: clip` на `.page-hero` / `.sheet` (clip, не hidden — не створює scroll-контейнер).

**Вирізано як недостовірне / сміттєве.**
- Кольорова FEA-шкала «84→0 МПа» на кадрі 02 висіла поруч із сірим рендером без жодної
  епюри — легенда описувала те, чого на зображенні немає. Знесено.
- Накладка-штамп «ДСТУ ISO 128 · ФОРМАТ A4» на кадрі 03 — аркуш і так видно.
- `fea-01-navis-stress.webp` стояв на сторінці тричі (велика плита + мініатюра з підписом
  «Загальний вигляд розрахункової моделі» + стрічка inside). Дубль-мініатюру прибрано, 3→2.
- Три картки пакетів мали ідентичний рядок «ВАРТІСТЬ · Індивідуально» і три однакові CTA
  «Обговорити проєкт» — таблиця пакетів не давала жодної підстави для вибору.

**Замість вирізаного.**
- Кадр 02 отримав чесну розрахункову схему: стрілки прикладеної сили, що реально лягають
  на верхній вузол стійки, + штрихування жорсткого закріплення по базі. Без вигаданої легенди.
- Пакети: у рядку ціни тепер **термін** (3–5 днів / 2–3 тижні / від 3 тижнів — узято з FAQ
  на головній), CTA різні. Під сіткою — один рядок про те, що фіксованого прайсу немає.
- Нова секція `#next` «05 / Наступний крок» перед підвалом: бриф, Telegram, 3 кроки роботи.

**Достовірність чисел.** Аркуш `sheet-03.webp` того самого об'єкта має штамп «Листов 32»
і 27 позицій у специфікації, а картка сцени 03 заявляла 12 аркушів і 34 позиції (причому
34 дублювало «Деталей у збірці» з картки 01). Приведено до 32 і 27 — обидва перевіряються
по самому аркушу.

**Вирівнювання і подача.**
- `.disc__plate--shot` (16/9) — скріни SolidWorks більше не ріжуться кропом 4/3 навпіл.
- `.disc__plate--sheet` — аркуш показано повністю (`contain`), не зумом у таблицю.
  Увага: правило мусить стояти **після** `.disc__plate img`, інакше `cover` його перебиває.
- `.deliv__what { min-height }` — картки «на виході» однакової висоти (було 2 vs 3 рядки).
- `.disc__media { align-self: center }` на ≥961px. Пробував `position: sticky` — **не працює**:
  `body` має `overflow-y:auto`, а `.disc` лишається з residual `transform` від reveal-анімації;
  обидва вбивають sticky. Центрування ділить повітря 136/136 замість дірки внизу.
- Кадр 04 (студійний рендер із власним сірим градієнтом) flat-field'нуто в тон паперу,
  плюс кадри 03/04 отримали хайрлайн-край: тепер це свідомо «документи, покладені на аркуш».
- Inline-стилі з HTML винесено в класи (`.inside__intro`, `.disc__thumbs`, `.pack-note`, `.is-zoomable`).
- Кириличний шлях `assets/Процес розробки/photo_….jpg` → `assets/services/renders/keyshot-suspension-arm.webp`
  (73KB jpg → 43KB webp). Кирилиця в URL — ризик на хостингу/CDN.
- Перенумеровано секції 01–05 (було 02–05 без першої).

**Деплой.** `service-worker.js`: CACHE `ko-engineer-v2-2026.10` → `.11`. Без бампу
постійні відвідувачі отримали б старий CSS — саме на цьому я спіткнувся під час перевірки.

**Перевірено:** 1440 / 768 / 375 — нуль переповнень, `docSW == bodySW == innerW`,
консоль чиста, усі запити 200/304, мертвих якорів і битих зображень немає.

## 2026-09-18 — плоске меню на всіх сторінках + лід-магніт знято

**Меню.** Дві випадайки («Послуги» з 5 підпунктами і «Роботи» з 4) замінено на 6 плоских
посилань на всіх 6 UA-сторінках: Послуги · Портфоліо · Візуалізація · Креслення ·
Досягнення · Про мене. Випало «Процес» (жив лише як якір `/#process` на головній).
EN-меню вже було плоским — не чіпав.

- Активний пункт тепер клас `.nav__link.is-current` + `aria-current="page"`, а не
  inline `style="color:var(--drafting)"`, який був розкиданий по сторінках.
- Ширина: 6 пунктів на 1440 — 771px у рядку 1208px. На 1024–1240 додано стиснення
  (`gap: 18px`, `font-size: 10px`) — на 1040px шапка сходиться рівно в 957px без переносу.
  Нижче 1024 меню й так ховається в бургер.
- Мобільне меню: прибрано вкладений `mobile-menu__sub`, додано «Про мене».

**Лід-магніт знято повністю** (за рішенням Костянтина — «вікно та пдф нехай не вилазить
зовсім»). Модалка вискакувала сама на 55% скролу, при exit-intent і через 45 секунд.
Прибрано: секцію-банер, модалку і `<script>` на index.html + en/index.html,
видалено `js/lead-magnet.js`, вичищено мертвий CSS `.lead-banner*` (42 рядки) і
`.modal*` (55 рядків) зі style.css, а також весь блок випадайок із services.css (92 рядки).
`assets/lead-magnets/10-errors-metal-design.html` лишив на диску — на нього більше ніхто
не посилається, але сам PDF нікуди не дівся, якщо захочеш повернути механіку.

**Пастка, яку ледь не залишив:** `service-worker.js` тримав `/js/lead-magnet.js` у списку
`SHELL`. `cache.addAll()` атомарний — один 404 обвалює **весь** precache мовчки
(там стоїть `.catch(() => {})`). Прибрав із SHELL і перевірив, що кожен запис існує на диску.
CACHE бампнуто `.11` → `.12`.

**Перевірено:** усі 6 сторінок — 6 посилань, правильний активний пункт, нуль випадайок,
нуль inline-стилів у меню, нуль залишків лід-магніта; консоль чиста; `js/lead-magnet.js` → 404.

## 2026-09-19 — секцію «ПАКЕТИ» знято зі сторінки послуг

**Що прибрано.** Секція `#packages` («Як це купують» · DWG-205 · 3 картки-тарифи +
рядок про відсутність фіксованого прайсу) — 66 рядків із `services.html`. Рішення
Костянтина.

**Хвости, які тягнулися за нею:**
- Нумерація аркушів мала діру: `05 / НАСТУПНИЙ КРОК` → `04 / НАСТУПНИЙ КРОК`.
  Тепер 01 НАВІГАТОР · 02 ДИСЦИПЛІНИ · 03 ЗА ЛАШТУНКАМИ · 04 НАСТУПНИЙ КРОК.
- `.pack-note*` у `services.css` (21 рядок) більше ніде не використовувався — знято.
- `blog/index.html` тримав мертвий якір `/services.html#packages` у випадайці «Послуги»
  (разом із роздільником). Прибрано.
- `service-worker.js`: CACHE `.12` → `.13`, інакше клієнти тягли б стару `services.css`.

**Окремо помічено (не чіпав):** `blog/index.html` — єдина сторінка, яка досі має стару
випадайкову навігацію. Плоске меню 18.09 розкотили по 6 UA-сторінках, блог у той підрахунок
не потрапив. Меню там розходиться з рештою сайту.

**Перевірено в браузері:** `#packages` у DOM немає, eyebrow-нумерація 01–04 без діри,
`body.is-loaded` відпрацьовує, gsap + lenis живі, консоль чиста, нуль мертвих згадок
`packages` / `pack-note` по всьому `site/`.

## 2026-09-19 — кадр 03 сцени послуг: аркуш друкується плотером

**Чому взагалі чіпали.** Кадр 03 був найслабшим у сцені `#stage`: усі чотири стани
використовували один прийом (crossfade + scale 1.06→1), і на вертикальному A4 він
розсипався — величезні порожні поля з боків, аркуш просто «проявлявся».

**Знахідка, яка переписала задум.** Відкрив `sheet-01/02/03.webp` — це **специфікації**
(таблиці BOM), а не креслення з видами. Жодних розмірів, допусків і зварних швів там
немає. Тому початковий план з лупою 1:1 і виносним апаратом `Ø20 H7` / `▷6` неможливий —
збільшувати нічого. Натомість таблиця BOM виявилась ідеальним об'єктом саме для
плотерного розкриття: специфікація в реальності так і заповнюється, рядок за рядком.

**Що зроблено.**
- Кадр перемкнуто з `sheet-01` (майже порожня збірка) на **`sheet-03`** — «Закупка
  матеріалів», ~27 реальних позицій. Збігається з показниками картки: «Позицій у BOM 27»,
  «Лист 3 · Листов 32».
- Білий папір лягає на стіл (`y 24→0`, `scale .985→1`), далі шторка `.plot__mask` кольору
  паперу сходить донизу і відкриває друк. На її краю їде перо з кареткою і міткою
  `ЛІ.10.000`. Перо — **дитина шторки**, тож це одна трансформація, а не дві синхронні.
- Чотири виноски в бокові поля з реальними позиціями цієї ж специфікації
  (Труба 160×160×5 — 2.19 м.п.; Лист х/к 5 мм — 0.37 м²; Болт M12×25 DIN933 — 4 шт;
  Шпилька M16×350 — 2 шт). Кожна вистрілює рівно тоді, коли перо проходить її рядок.
- Штамп обводиться червоною рамкою (SVG `pathLength=1` + `stroke-dashoffset`) — той самий
  прийом, що вже стояв на кадрі 02, тож секція заговорила однією мовою.

**Пастка №1 — колізія імен.** Перший заход назвав блок `.sheet`. Але `.sheet` — це
**класна секція сайту** (`<section class="sheet">`, `style.css:1320`). Рулсет накидав би
всім секціям `aspect-ratio: 2200/3112`, білий фон, рамку й тінь. Перейменовано на `.plot`.
Перевірено: `section.sheet` зберіг `padding-top: 126px`, `aspect-ratio: auto`, нуль рамки.

**Пастка №2 — тайминг.** Перша збірка мала `RUN = HOLD * 0.66`: штамп домальовувався за
0.03 до початку кросфейду на кадр 04, тобто готовий аркуш не встигали побачити. Стиснуто
до `HOLD * 0.50` + коротший штамп. Тепер усе завершується на `t≈4.42`, а перехід стартує
на `t≈4.68` — чверть кадру аркуш просто стоїть надрукований.

**Пастка №3 — SW під час тесту.** Сторінка тягнула стару `services.css` із кеша
service-worker'а, і всі правки «не діяли». CACHE бампнуто `.13` → `.14`.

**Заміряна хореографія** (керував таймлайном напряму, без лагу scrub):

| t | друк | перо | виноски | штамп | аркушів |
|---|---|---|---|---|---|
| 3.65 | 0% | — | — | — | 0 |
| 3.85 | 41% | ● | 1 | — | 9 |
| 4.00 | 71% | ● | 1-2 | — | 16 |
| 4.15 | 100% | зійшло | 1-3 | — | 22 |
| 4.30 | 100% | — | 1-4 | ✓ | 29 |
| 4.42 | 100% | — | всі | ✓ | 32 |
| 4.68 | → кросфейд на кадр 04 |

**Вимоги виконано:** рухаються тільки `transform` і `opacity` — шторка й перо лишаються
на композиторі, растр не перемальовується жодного кадру. Консоль чиста. На 390px аркуш
влазить у рамку, горизонтального скролу немає, виноски приховані нижче 1200px (поля вужчі
за виноску). Гілка без GSAP / `prefers-reduced-motion` додає `.plot.is-static` — аркуш
просто надрукований: шторка зведена, виноски й штамп на місці.

**Що лишилось відкритим:** на мобільному рамка сцени всього ~312px заввишки, тож сам
друк читається, а текст специфікації — ні. Це обмеження layout'у сцени, не цього прийому.
І варіант C (лупа 1:1 з виносним апаратом) чекає на один ассет — аркуш **деталювання**
з видами, розмірами й швами.

## 2026-09-20 — сторінка послуг: дисципліни, стрічка, відомість

Чотири задачі від Костянтина одним заходом.

### Знахідка, яка зняла блокер із попередньої сесії

У `site/assets/Зразки креслень/` лежить **`Баскетбольна стійка з підйомником для
регулювання по висоті.pdf` — 32 аркуші**, рівно той комплект, на який посилаються
штампи («Листов 32») у сцені `#stage`. Там не лише специфікації: p6 — складальне
з номерами позицій і виносками, p7–p8 — габаритні з розмірами, p10–p13 — вузли
з розрізами A-A, Б-Б і виносними видами K(1:2), M(1:2), P(1:5), p14–p32 —
деталювання позицій з розмірами й фасками.

Тобто **варіант C для кадру 03 (лупа 1:1 з виносним апаратом) більше не
заблокований** — реальна геометрія з допусками є на диску. Минулого разу я шукав
її лише у вже сконвертованих `sheet-01..03.webp`, які виявилися трьома
специфікаціями, і зробив висновок по трьох файлах замість того, щоб відкрити
першоджерело. Урок: перш ніж казати «ассета немає», відкрий PDF, а не webp.

### 1. Дисципліни: текст залипає, кадри йдуть повз

Було: два стовпці, `align-items: start`, медіа штучно центрувалася по колонці —
під картинкою зяяла дірка на пів екрана (саме її Костянтин і зняв скріном).

Стало: кожна з чотирьох дисциплін отримала **по три кадри** одне під одним, текст
`position: sticky` — стоїть, поки повз нього проходить уся колонка. Заміряно на
1440×900: текст 713–786px, медіа-колонка 1131–1891px, тож хід залипання є всюди.

**Причина, через яку sticky «був неможливий», виявилась інша.** У коді стояв
коментар, що винен residual transform від reveal-анімації. Насправді transform
там одиничний і нешкідливий, а вбивав sticky `overflow-x: hidden` на `html` і
`body`: hidden примусово вмикає `overflow-y: auto`, елемент стає скрол-контейнером
і залипання в усіх нащадках помирає. Замінено на `overflow-x: clip` (з `hidden`
першим рядком як запасним) — горизонталь тримається так само, скрол-контейнера
немає. Це правка в `style.css`, тобто **діє на весь сайт**: sticky тепер можна
використовувати будь-де.

**Обрізка.** Перша збірка ставила кадру `height` = висоті тексту і `object-fit:
cover` — дослівно те, що просив Костянтин («висота тексту як висота малюнка»).
На екрані це розвалилось: 16:9 скрін у колонці 620px не може бути 720px заввишки,
не втративши дві третини кадру. Геометрія не сходиться в принципі. Тому кожна
плита отримала власну пропорцію файлу через inline `--ar: W / H` і `object-fit:
contain` — нуль обрізки, кадр настільки великий, наскільки дозволяє колонка.
Костянтину сказано прямо, що саме тут відступив від букви прохання і чому.

**Пастка з колонками.** `.disc--flip` міняє порядок, тож у перевернутих аркушах
медіа опинялась у вузькій колонці (443px проти 625px). Додано окремий
`grid-template-columns` для `--flip`.

**Пастка з висотою тексту.** Залиплий блок, вищий за екран, ховає власний низ
назавжди — а там кнопка CTA. Додано `initDiscSticky()`: якщо текст не вміщається
(`innerHeight - --disc-top - 16`), блок отримує `.is-unstuck` і йде звичайним
потоком. Щоб JS читав обчислений `--disc-top`, а не сирий рядок `clamp(...)`,
змінну зареєстровано через `@property` (з фолбеком на стелю 104px). Ритм тексту
підтиснуто (lead 30→24, flow 32→26, рядок 12→10, deliv 30→24), `--disc-top`
опущено до `clamp(76px, 9vh, 104px)` — на 1440×900 усі чотири липнуть, на 1440×680
всі чотири чесно відмикаються.

### 2. Робоча документація: паспорт на аву

Ведучий кадр — **перша сторінка паспорта** (`ПАСПОРТ · Стенд баскетбольний з
регулюванням висоти · ЛІ.10.000` з ізометрією виробу), далі складальне з позиціями
і аркуш деталювання з розрізами. Витягнуто з PDF через PyMuPDF на 200 dpi:
`doc-01-passport.webp` 1654×2339 (75 KB), `doc-02-assembly.webp` і
`doc-03-details.webp` 3308×2339 (230 / 106 KB). Галерею `doc-set` додано в
`services-data.js`, лайтбокс їх бачить.

### 3. Стрічка SolidWorks: вільне гортання

- Кадр збільшено з `clamp(280px, 40vw, 560px)` до `clamp(300px, 52vw, 780px)`
  (заміряно 749px на 1440). Джерела 1920×1032, тож на 780px запас ще є.
- `scroll-snap-type: x mandatory` знято — снап не давав дотягнути кадр туди, куди
  хочеш.
- Тягнеться мишею: pointer-події + `setPointerCapture`, курсор `grab`/`grabbing`,
  після перетягування >6px клік глушиться в capture-фазі, щоб не відкривався
  лайтбокс.
- Колесо над стрічкою гортає її по горизонталі. **На краях подія не
  перехоплюється** — спливає далі й Lenis гортає сторінку, тож стрічка не стає
  пасткою для скролу. Всередині — `preventDefault` + `stopPropagation`, інакше
  Lenis смикав би ще й сторінку.

### 4. Блок під заголовком: відомість замість метрик

Було чотири однакові комірки «велика червона цифра + mono-підпис» — рівно той
`hero-metric template`, який скіл `design-taste` тримає в списку заборонених.

Стало `.register` — **індекс комплекту**: номер, назва, крапковий виносний поводок
(конвенція змісту креслярського альбому), склад того, що лягає на диск
(`SLDASM · STEP · STL`), стрілка. На наведенні поводок перекриває суцільна червона
лінія через `scaleX(0→1)`, назва зсувається на 6px, номер і стрілка червоніють.
Рядки — справжні посилання у `#modeling`, `#analysis`, `#drawings`, `#visual`.

Контраст заміряно: номер 4.84:1, назва 17.25:1, склад 10.18:1 — усе вище 4.5.
Рядок 64px (тач-ціль ок). На <680px поводок ховається, склад іде другим рядком.
Є гілка `prefers-reduced-motion`. `:focus-visible` дає рамку drafting-red.

**Пастка з іменем.** Клас `.page-hero__stats` живе ще на 7 сторінках — його CSS не
чіпав узагалі, замінив лише розмітку в `services.html`. Ім'я `.register`
перевірено на колізії (після історії з `.sheet`).

### Перевірено

1440×900: усі 4 дисципліни sticky, медіа-колонки рівні 625px, нуль обрізки
(пропорція боксу = пропорція файлу), нуль битих зображень, консоль чиста.
1440×680: запобіжник знімає залипання на всіх чотирьох.
390×844: горизонтального скролу немає, відомість 72px/рядок зі складом другим
рядком, дисципліни в одну колонку, кадр стрічки 300px.

CACHE `.14` → `.20`.

### Відкрите

- Кадр 03 у `#stage` досі друкує **специфікацію**. Тепер, коли знайдено аркуші
  з розмірами й розрізами, туди напрошується або заміна аркуша, або варіант C.
- `blog/index.html` — єдина сторінка зі старою випадайковою навігацією.
